replicaCount: 2

image:
  repository: ecommerceacrregistry.azurecr.io/ecommerce-api
  pullPolicy: IfNotPresent
  tag: 5c8dca74f082c95224623fc3825c9018bbd2f1cb

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: 
      paths:
        - path: /
          pathType: ImplementationSpecific

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

env:
  PORT: "3000"
  NODE_ENV: "production"
